---
import { cn } from '@utils/cn.ts'

interface Props {
	className?: string
}

const { className } = Astro.props
---

<div
	class={cn(
		'px-2 flex items-center text-foreground gap-4 font-semibold',
		className
	)}
>
	<slot />
</div>
