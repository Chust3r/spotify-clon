---
interface Props {
	id: string
	title: string
	thumbnail: string
	author: string
	type: string
}

const { thumbnail, title, author, type, id } = Astro.props
---

<a
	href={`/playlist/${id}`}
	class='w-full h-16 rounded-md bg-gradient-to-r from-neutral-800/5 from-[5%] hover:bg-neutral-800/50 focus:bg-neutral-800/50 flex items-center px-2 gap-3 transition-all duration-200 relative'
	role='button'
>
	<img src={thumbnail} alt='' class='w-12 h-12 rounded' />
	<div class='absolute inset-0'></div>
	<div class='flex flex-col text-white justify-center'>
		<p class='font-light'>{title}</p>
		<p class='text-sm flex gap-1 text-foreground'>
			<span>{type}</span><span class='font-black'>·</span><span
				>{author}</span
			>
		</p>
	</div>
</a>
